datasets>=2.15.0
numpy>=1.24.0
matplotlib>=3.7.0
tiktoken>=0.5.0
# Install the appropriate torch build for your system (CPU or CUDA)
# For CUDA builds see https://pytorch.org/get-started/locally/
torch>=2.1.0
# Optional but recommended for progress bars
# tqdm is pulled transitively by datasets, but we pin it explicitly
tqdm>=4.66.0
